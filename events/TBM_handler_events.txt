### The Blocker Mod - Handler Events
#
# This file handles adding and removing non-natural features during gameplay.

namespace = tbm_handler

### Variables
@miners = 0
@mines = 0
@sinkholes = 0

### INDEX ###
#	-- Init Fixes --
#	1. Scripted Planets Handler
#	2. Advanced Start Feature Fixer
#	3. Aquatics Fix
#
#	-- Generators --
#	4. Sprawling Slum
#	6. Industrial Wasteland
#	8. Colonial Settlement
#	10. Frontier Farmland
#	12. New Mining Prospects
#	15. Dustbowls
#	17. Sinkholes

#	-- Event Listeners --
#	20. False Prospects / Quality Find
#	21. Central Power Nexus Breaks
#
#	-- Additional Generators --
#	24. Farmland / Moisture Farming
#
#	-- Colony Listeners --
#	99. Bombardment Listener (adds Tomb features)
#	100. Pioneering Colonists
#	103. Wild Worlds
#	105. Pre-FTLs Achieve FTL
#	106. Gestalt Pioneering Colonists
#	200. Terraforming Handler
#	301. Wenkwort Handler

#1. Scripted Planets Handler
### Many scripted planets in vanilla call clear_blockers on init, or add vanilla deposits that will either be broken or unbalanced by the mod. This event is fired on_game_start and checks for specific scripted planets. It may cause a slight lag on lower-end machines when the game is first started.

event = {
	id = tbm_handler.1

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		
		log = "tbm_handler.1 - Scipted Planets"
		
		### Reroll scripted planets
		scripted_planet_reroll = yes
		
		### FALLEN EMPIRE WORLDS
		fix_fallen_empire_worlds = yes

		### IMPERIAL FIEFDOM FIX (overlord_system_x_init)
		if = {
			limit = {
				any_country = { has_origin = origin_imperial_vassal }
			}
			
			# IF Overlord
			event_target:origin_imperial_homeland_country = {
				
				# Empire Capital
				event_target:origin_imperial_homeland = {
					clear_deposits = yes
					clear_blockers = yes

					generate_start_deposits_and_blockers = yes
					fix_advanced_start_empire_features = yes
					while = {
						count = 6
						add_district_and_planet_size_if_needed_effect = {
							district = district_city
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_mining
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_farming
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_generator
						}
					}
					
					# IF Overlord features
				}
				
				# Overlord Colonies
				set_variable = {
					which = x
					value = trigger:num_owned_planets
				}

				ordered_owned_planet = {
					position = 1
					order_by = x
					limit = {
						NOT = { is_same_value = event_target:origin_imperial_homeland }
					}
					clear_deposits_without_effects = yes
					while = {
						count = 3
						add_district_and_planet_size_if_needed_effect = {
							district = district_city
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_mining
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_farming
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_generator
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_industrial
						}
					}
					reroll_deposits = yes
				}
				ordered_owned_planet = {
					position = 2
					order_by = x
					limit = {
						NOT = { is_same_value = event_target:origin_imperial_homeland }
					}
					clear_deposits_without_effects = yes
					while = {
						count = 2
						add_district_and_planet_size_if_needed_effect = {
							district = district_city
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_mining
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_farming
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_generator
						}
						add_district_and_planet_size_if_needed_effect = {
							district = district_industrial
						}
					}
					reroll_deposits = yes
				}
			}

			# Vassal 1
			if = {
				limit = {
					exists = event_target:origin_imperial_homeland_country_2
				}
				event_target:origin_imperial_homeland_country_2 = {
					every_owned_planet = {
						clear_deposits = yes
						clear_blockers = yes
						generate_start_deposits_and_blockers = yes
					}
				}
			}

			# Vassal 2
			if = {
				limit = {
					exists = event_target:origin_imperial_homeland_country_3
				}
				event_target:origin_imperial_homeland_country_3 = {
					every_owned_planet = {
						clear_deposits = yes
						clear_blockers = yes
						generate_start_deposits_and_blockers = yes
					}
				}
			}

			# Vassal 3
			if = {
				limit = {
					exists = event_target:origin_imperial_homeland_country_4
				}
				event_target:origin_imperial_homeland_country_4 = {
					every_owned_planet = {
						clear_deposits = yes
						clear_blockers = yes
						generate_start_deposits_and_blockers = yes
					}
				}
			}

			# Vassal 4
			if = {
				limit = {
					exists = event_target:origin_imperial_homeland_country_5
				}
				event_target:origin_imperial_homeland_country_5 = {
					every_owned_planet = {
						clear_deposits = yes
						clear_blockers = yes
						generate_start_deposits_and_blockers = yes
					}
				}
			}
		}		
	}
}

### 2. Advanced Start Feature Fixer
#	Advanced Start empires need to reroll features after vanilla init or else they'll have no features and will be crippled by unemployment.
event = {
	id = tbm_handler.2

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		
		log = "tbm_handler.2 - Advanced Starts"
		
		every_country = {
			limit = {
				has_country_flag = is_advanced_start_AI
			}

			every_owned_planet = {
				limit = {
					is_capital = yes
				}
				
				### Get rid of unwanted fish. For real this time.
				every_deposit = {
					remove_deposit = yes
				}

				generate_start_deposits_and_blockers = yes	# Re-runs basic TBM feature init.
				fix_advanced_start_empire_features = yes	# Add Advanced features and clean-up
			}

			every_owned_planet = {
				limit = {
					NOT = { is_capital = yes }
				}

				clear_deposits = yes
				reroll_deposits = yes
			}
		}
	}
}

### 3. Aquatic Fix
#	This is a hack to fix a peristent bug with Aquatic empires where they would get appropriate features but no districts due to some unknown problem with generate_start_buildings_and_districts that TBM does not patch. Until I can find it, this should reset their planets properly, but may remove other modded features.
event = {
	id = tbm_handler.3

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_playable_country = {
			limit = {
				OR = {
					is_anglers_empire = yes
					has_trait = trait_aquatic
				}
			}

			every_owned_planet = {
				remove_all_buildings = yes
				generate_start_buildings_and_districts = yes
			}

			if = {
				limit = { has_country_flag = is_advanced_start_AI }
				capital_scope = {
					planet_event = {
						id = game_start.22	# Re-do the advanced building spawn
					}
				}
			}
		}

		### Little clean-up due to problem with remove_deposit = yes - you can only get this if we clear Public Mining Concerns; sometimes this happens if a Prosperous Unification empire is also Aquatic or an Advanced Start
		every_country = {
			every_owned_planet = {
				if = {
					limit = {
						has_modifier = privatizations
					}
					remove_modifier = privatizations
				}
			}
		}
	}
}


### 4. Slum Handler (on_colony_yearly_pulse)
planet_event = {
	id = tbm_handler.4
	hide_window = yes

	is_triggered_only = yes

	pre_triggers = {
		has_owner = yes
	}

	# Several pre-reqs here that basically amount to crummy urban living; should be less common in late-game post-scarcity economies but a challenge as you grow quickly.
	trigger = {
		exists = owner
		owner = { is_country_type = default }
		
		years_passed > 5
		colony_age_years > 20
		
		num_districts = {
			type = district_city
			value >= 1
		}
		count_deposits = {
			type = d_tbm_slums
			count < 6
		}

		# Got to have one each of sprawl and poor conditions
		OR = {
			free_amenities < 500
			free_housing < 500
		}
		OR = {
			free_district_slots < 1
			num_districts = {
				type = district_city
				value > 3
			}
		}	
	}

	immediate = {
		
		random_list = {
			# Slums generated, more likely if conditions are already bad.
			15 = {
				modifier = {
					factor = 1.5
					exists = leader
					leader = {
						has_total_skill >= 3
						leader_class = official
					}
				}
				modifier = {
					factor = 2.5
					any_owned_pop_group = {
						has_low_living_standard_type = yes
					}
				}
				modifier = {
					factor = 1.25
					owner = {
						has_deficit = consumer_goods
					}
				}
				modifier = {	# Incentivize local food!
					factor = 1.25
					planet_resource_compare = {
						resource = food
						value < 0
					}
				}
				modifier = {
					factor = 1.25
					owner = {
						OR = {
							has_authority = auth_corporate
							has_authority = auth_oligarchic
						}
					}
				}
				planet_event = {
					id = tbm_handler.5
					days = 180
					random = 90
				}
			}
			# No slums this time.
			85 = {
				modifier = {
					factor = 2
					owner = {
						OR = {
							has_civic = civic_idealistic_foundation
							has_civic = civic_beacon_of_liberty
						}
					}
				}
				modifier = {
					factor = 1.5
					exists = leader
					leader = {
						has_total_skill >= 4
						leader_class = official
					}
				}
				modifier = {
					factor = 2
					exists = leader
					leader = {
						OR = {
							has_trait = leader_trait_corrupt
							has_trait = leader_trait_corrupt_2
						}
					}
				}
			}
		}

	}
}

### 5. Slum Generator
planet_event = {
	id = tbm_handler.5
	hide_window = yes

	is_triggered_only = yes

	immediate = {
		add_deposit = d_tbm_slums	# Gestalts need to get custom slums eventually.
		create_message = {
			type = "SLUM_GENERATOR"
			localization = SLUM_GENERATOR
			days = 30
			target = this
		}
	}
	
}

### 6. Industrial Wasteland Handler (on_colony_yearly_pulse)
planet_event = {
	id = tbm_handler.6
	hide_window = yes

	is_triggered_only = yes

	trigger = {
		exists = owner
		owner = { is_country_type = default }
		
		is_artificial = no
		num_districts = {
			type = district_city
			value >= 1
		}
		has_industrial_zoning = yes
		years_passed > 10	# Grace period for homeworlds; we do want them to struggle with these, but there's a lot going on at game start with other features.

		# Got to have one each of sprawl and poor business conditions
		OR = {
			free_amenities < 800
			planet_crime > 10
		}

		# Partly to punish highly-specialized planets of any kind, but okay as long as you don't stuff it.
		AND = {
			free_district_slots < 2
			OR = {
				num_districts = {
					type = district_mining
					value > 3
				}
				num_districts = {
					type = district_generator
					value > 3
				}
			}
		}
	}

	immediate = {
		
		random_list = {
			# Wastelands generated, more likely with some other factors.
			15 = {
				modifier = {
					factor = 2
					owner = {
						OR = {
							has_authority = auth_corporate
							has_authority = auth_oligarchic
							has_authority = auth_dictatorial
						}
					}
				}
				modifier = {
					factor = 2
					owner = {
						OR = {
							has_deficit = consumer_goods
							has_deficit = minerals
							has_deficit = energy
							has_deficit = alloys
						}
					}
				}
				modifier = {
					factor = 2
					planet_crime > 20
				}


				planet_event = {
					id = tbm_handler.7
					days = 180
					random = 180
				}
			}
			# No wasteland this time.
			85 = {
				modifier = {
					factor = 1.5
					owner = { has_valid_civic = civic_merchant_guilds }
				}

			}
		}

	}
}

### 7. Industrial Wasteland Generator
planet_event = {
	id = tbm_handler.7
	hide_window = yes

	is_triggered_only = yes

	immediate = {
		add_deposit = d_tbm_industrial_wasteland
		create_message = {
			type = "INDUSTRIAL_WASTELAND"
			localization = INDUSTRIAL_WASTELAND
			days = 30
			target = this
		}
	}
}

### 8. Colonial Settlement Handler (on_colony_yearly_pulse, colony_age < 20)
planet_event = {
	id = tbm_handler.8

	hide_window = yes

	is_triggered_only = yes

	pre_triggers = {
		has_owner = yes
		is_occupied_flag = no	# Occupying forces not as keen on new settlements as they are on new power plants
	}

	trigger = {
		exists = owner
		owner = { is_country_type = default }
		
		is_artificial = no
		is_starting_planet = no
		OR = {
			has_building = building_colony_shelter
			has_building = building_capital
		}
		owner = { is_gestalt = no }
		colony_age_years < 20
	}

	immediate = {
		random_list = {
			# Colonial Settlement - adds feature, housing, and colonist jobs.
			5 = {
				modifier = {
					factor = 2
					pop_amount > 800	# Should result in a few more settlements at that tight spot right before you get Planetary Admin.
				}

				modifier = {
					factor = 4
					num_assigned_jobs = {
						job = colonial_deputy
						value >= 50
					}
				}

				# Reduce likelihood if we have settlements already, we don't want these getting weedy.
				modifier = {
					mult = 0.5
					count_deposits = {
						type = d_settlement
						count >= 2
					}
				}

				modifier = {
					mult = 0.5
					pop_amount > 1800
				}

				modifier = {
					factor = 0
					count_deposits = {
						type = d_settlement
						count >= 4
					}
				}

				planet_event = {
				 	id = tbm_handler.9
				 	days = 180
					random = 90
				}
			}
			95 = {} 	# No settlement this year, busy having babies.
		}
	}
}

### 9. Colonial Settlement Generator
planet_event = {
	id = tbm_handler.9

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		add_deposit = d_settlement
		create_message = {
			type = "COLONIAL_SETTLEMENT"
			localization = COLONIAL_SETTLEMENT
			days = 30
			target = this
		}
	}
}

### 10. Frontier Farmland Handler (on_colony_yearly_pulse)
planet_event = {
	id = tbm_handler.10

	hide_window = yes
	is_triggered_only = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		exists = owner
		owner = { is_country_type = default }
		
		is_artificial = no
		NOR = {
			is_planet_class = pc_relic
			is_planet_class = pc_nuked
		}
		is_starting_planet = no
		owner = { is_gestalt = no }
		num_free_districts = {
			type = district_city
			value > 3	# These are meant to be mostly good, and get very annoying if they spawn whyen you're trying to clear space for something else, like actual farms.
		}
		num_free_districts = {
			type = district_farming
			value > 1
		}
		count_deposits = {
			type = d_frontier_farmstead
			count < 4
		}
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					add = 5
					num_free_districts = {
						type = any
						value > 7
					}
				}
				modifier = {
					add = 5
					# Planets where the ranchin' is good
					OR = {
						is_dry = yes
						is_planet_class = pc_gaia
					}
				}

				modifier = {
					add = 10
					num_assigned_jobs = {
						job = colonial_pioneer
						value >= 100
					}
				}

				modifier = {
					add = 10
					owner = { has_valid_civic = civic_agrarian_idyll }
				}
				
				# Don't spawn rural farmsteads on worlds that are getting denser or already have a lot of farmsteads.
				modifier = {
					factor = 0.5
					count_deposits = {
						type = d_frontier_farmstead
						count >= 2
					}
				}
				modifier = {
					factor = 0.5
					pop_amount > 2000
				}
				modifier = {
					factor = 0
					OR = {
						has_building = building_major_capital
						pop_amount > 400
					}
				}

				planet_event = {
					id = tbm_handler.11
					days = 180
					random = 90
				}
			}
			
			90 = {}
		}
	}
}

### 11. Frontier Farmland Generator
planet_event = {
	id = tbm_handler.11

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		add_deposit = d_frontier_farmstead
		create_message = {
			type = "FRONTIER_FARMLAND"
			localization = "FRONTIER_FARMLAND"
			days = 30
			target = this
		}
	}
	
}

### 12. Mining Prospects Handler (on_colony_yearly_pulse)
planet_event = {
	id = tbm_handler.12

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
		has_ground_combat = no
		is_occupied_flag = no
		is_homeworld = no
	}

	trigger = {
		years_passed > 5
		is_starting_planet = no
		is_artificial = no
		NOT = { is_same_value = owner.capital_scope }
		NOT = { has_planet_flag = prospects_done }
		colony_age_years > 2
		colony_age_years < 50

		NOR = {
			is_planet_class = pc_relic
			is_planet_class = pc_nuked
			is_planet_class = pc_nuked_tbm		# Should never happen but just in case
		}

		check_variable = {
			which = @prospecting_chance
			value <= 3
		}
	}

	immediate = {
		random_list = {
			# Quite rare, but higher on mining worlds and new colonies to account for natural exploration.
			2 = {
				modifier = {
					add = 3
					colony_age_years < 10
				}

				modifier = {
					add = 5
					num_assigned_jobs = {
						job = miner
						value > 6
					}
				}

				modifier = {
					add = 5
					OR = {
						num_assigned_jobs = {
							job = colonial_prospector
							value > 50
						}
						num_assigned_jobs = {
							job = prospecting_drone
							value > 50
						}
					}
					
				}
				
				planet_event = {
					id = tbm.300
					days = 180
					random = 180
				}
			}

			95 = {}
		}
	}

}

### 15. Dustbowl Handler (Overfarming Prototype) (on_colony_ten_year_pulse)
planet_event = {
	id = tbm_handler.15

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		exists = owner
		owner = { is_country_type = default }
		
		years_passed > 50	# Partly because you're unlikely to have Soil Remediation that quickly and without it, Dustbowls can choke your ag colonies rather quickly before any more complex economy is set up. The intent of these is more to target breadbasket worlds in a larger empire.

		colony_age_years > 40
		OR = {
			num_districts = {
				type = district_farming
				value > 6
			}
			num_districts = {
				type = any
				value > 12
			}
		}
		any_deposit = {
			is_farmland_feature = yes
			can_be_exhausted = yes
		}
		
		# If we're just maintaining land but not actually growing on it, give some amnesty; this also helps alleviate situations where Anglers are getting Dustbowls
		num_assigned_jobs = {
			job = farmer
			value > 400
		}

		NOT = { has_planet_flag = soil_remediation }

		NOT = {
			check_variable = {
				which = dustbowl_counter
				value > 5
			}
		}
	}

	immediate = {	
		random_list = {
			10 = {
				planet_event = {
					id = tbm_handler.16
					days = 180
					random = 180
				}
			}
			90 = {
				# Much less chance for lower-density agrarians to exhaust their planets
				modifier = {
					factor = 4
					owner = { has_valid_civic = civic_agrarian_idyll }
				}
			}
		}
	}
}

### 16. Dustbowl Generator
planet_event = {
	id = tbm_handler.16

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		if = {
			limit = {
				num_districts = {
					type = district_farming
					value > 0
				}
			}
			remove_district = district_farming
		}
		else = {
			# Do nothing, let the feature removal take care of it.
		}

		random_deposit = {
			limit = {
				is_farmland_feature = yes
				can_be_exhausted = yes
			}
			remove_deposit = yes
		}

		add_deposit = d_dustbowl

		change_variable = {
			which = dustbowl_counter
		}
		
		create_message = {
			type = "DUSTBOWL_GENERATOR"
			localization = message_dustbowl_generator
			days = 30
			target = this
		}
	}
}

### 17. Sinkhole Spawner
planet_event = {
	id = tbm_handler.17

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		exists = owner
		owner = { is_country_type = default }
		OR = {		
			# Sinkholes make more sinkholes
			has_deposit = d_deep_sinkhole

			# But can also be called after you clear them.
			has_planet_flag = occasional_sinkholes
		}
		NOT = { owner = { has_country_flag = safe_from_sinkholes } }
		NOT = {
			count_deposits = {
				count = 3	# Failsafe for this creating too many sinkholes, in case the event chain bugs out for any reason.
				type = d_deep_sinkhole
			}
		}
	}

	immediate = {
		if = {
			limit = {
				has_planet_flag = sinkhole_causes # Prevents firing the event to give the project when you already have it.
				NOT = { has_planet_flag = sinkhole_investigated }
				
				# And only fire Sinkhole Causes after we've seen a couple sinkholes
				check_variable = {
					which = @sinkholes
					value > 3
				}
			}
			
			# Sinkhole Cause Investigation
			planet_event = {
				id = tbm.82
				days = 360
				random = 180
			}
		}

		# 20% chance for Sudden Sinkhole this year
		else = {
			random_list = {
				20 = {
					modifier = {
						factor = 1.5
						has_deposit = d_deep_sinkhole
					}
					modifier = {
						factor = 1.5
						count_deposits = {
							count > 1
							type = d_deep_sinkhole
						}
					}
					modifier = {
						factor = 0.5 	# Less common if you've chosen to live with them; I have a heart
						has_planet_flag = occasional_sinkholes
					}
					modifier = {
						factor = 0
						has_planet_flag = sinkhole_investigated
					}
					
					planet_event = {
						id = tbm.81
						days = 360
						random = 180
					}

					change_variable = {
						which = @sinkholes
						value = 1
					}
				}
				80 = {}
			}
		}
	}

	after = {
		random_list = {
			40 = {
				set_planet_flag = sinkhole_causes 	# Getting tired of sinkholes, can investigate next time.
			}
			60 = {}
		}
	}
}


### 20. False Prospects Handler (on_district_complete)
#	For event, see TBM_02_events.txt
planet_event = {
	id = tbm_handler.20

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
		has_ground_combat = no
		is_occupied_flag = no
	}

	trigger = {
		is_artificial = no
		NOT = { has_planet_flag = mining_bust } 	# Helps avoid over-firing when constructing a lot of districts.
		last_district_changed = district_mining
		any_deposit = {
			has_mineral_seam = yes
		}
		num_districts = {
			type = district_mining
			value > 1	# 1 because last_district_changed can check after the district is removed; should avoid situations where a planet removes its last mining district and then gets False Prospects.
		}
	}

	immediate = {
		random_list = {
			5 = {
				
				# More likely on planets with lots of miners, all trying to strike it gold
				modifier = {
					factor = 2
					num_assigned_jobs = {
						job = miner
						value > 2
					}
				}
				modifier = {
					factor = 2
					num_assigned_jobs = {
						job = miner
						value > 4
					}
				}

				planet_event = {
					id = tbm.88
					days = 720
					random = 180
				}
		
				set_timed_planet_flag = {
					flag = mining_bust
					years = 10	# Takes a good while for people to forget
				}
			}
			80 = {}
		}
	}
}

### 21. Central Power Nexus Breaks
#	Fires any time planet devastation is greater than 80
planet_event = {
	id = tbm_handler.21

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		planet_devastation > 80
		has_deposit = d_central_power_nexus
		owner = { is_machine_empire = yes }	# Don't apply nasty debuff if owner isn'[t a machine.
	}

	immediate = {
		add_modifier = {
			modifier = damaged_power_nexus
			years = 20
		}
	}
	
}


### 24. Farming Handler
planet_event = {
	id = tbm_handler.24

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
		has_ground_combat = no
		is_occupied_flag = no
		is_homeworld = no
	}

	trigger = {
		is_artificial = no
		NOR = {
			is_planet_class = pc_relic
			is_planet_class = pc_nuked
		}
		is_starting_planet = no
		is_homeworld_or_lost_colony = no
	}

	immediate = {
		random_list = {
			33 = {
				modifier = {
					factor = 2
					OR = {
						any_deposit = {
							is_farmland_feature = yes
						}
					}
				}
				
				planet_event = {
					id = tbm_handler.25
					days = 180
					random = 180
				}
			}

			66 = {

			}
		}
	}
}

### 25. Farming Generator
planet_event = {
	id = tbm_handler.25

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		if = {
			limit = {
				OR = {
					owner = { has_valid_civic = civic_agrarian_idyll }
					colony_type = col_farming
					num_free_districts = {
						type = district_city
						value > 3
					}
					NOT = { owner = { has_technology = tech_gene_crops }}
				}
			}

			random_list = {
				5 = {
					if = {
						limit = {
							OR = {
								is_planet_class = pc_desert
								is_planet_class = pc_arid
							}
						}
						random_list = {
							50 = {
								add_deposit = d_homestead_farms

								create_message = {
									type = "FARMING_RURAL"
									localization = message_farming_rural
									days = 30
									target = this
								}
							}

							50 = {
								add_deposit = d_moisture_farms

								create_message = {
									type = "FARMING_RURAL"
									localization = message_farming_moisture
									days = 30
									target = this
								}
							}
						}
					}
					else = {
						add_deposit = d_homestead_farms

						create_message = {
							type = "FARMING_RURAL"
							localization = message_farming_rural
							days = 30
							target = this
						}
					}
				}

				95 = {}
			}
		}

		else = {
			random_list = {
				5 = {
					modifier = {
						factor = 0
						count_deposits = {
							count = 2
							type = d_urban_aeroponics
						}
					}
					
					add_deposit = d_urban_aeroponics

					create_message = {
						type = "FARMING_URBAN"
						localization = message_farming_urban
						days = 30
						target = this
					}
				}

				95 = {}
			}
		}
	}
}

### 99. Bombardment Listener
#	Tracks planets being bombarded so that, if switched to Tomb Worlds, they get features appropriate for them.
#	Sets flag on_planet_bombarded because on_planet_class_changed is called in too many other places, including game start.

planet_event = {
	id = tbm_handler.99

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		exists = owner
		planet_devastation >= 100
		has_orbital_bombardment_stance = armageddon
	}

	immediate = {
		planet_event = {
			id = tbm_handler.110
			days = 2	# To delay after the planet class change so we can just reroll deposits and let the drop_weights give us some variety.
		}
	}
}

### 100. Colonization Handler
#	on_colonized
planet_event = {
	id = tbm_handler.100

	hide_window = yes
	is_triggered_only = yes

	pre_triggers = {
		is_homeworld = no
		is_capital = no
	}

	trigger = {
		NOT = { has_planet_flag = lost_colony_planet }	# Do not spawn this on LC parents or their colonies generated at game start.

		is_artificial = no

		# owner = {
		# 	num_owned_planets < 6	# Only applied for first few colonies
		# }
		
		num_free_districts = {
			type = district_city
			value <= 0
		}
	}

	immediate = {
		this = {
			if = {
				limit = {
					owner = { is_gestalt = yes }
				}
				planet_event = {
					id = tbm_handler.106
				}
			}
			else = {
				planet_event = {
					id = tbm_handler.101
				}
			}
		}
	}

}

### 101. Pioneering Colonists
#	Exists to help with clarity and difficulty when colonizing very inhospitable worlds.
planet_event = {
	id = tbm_handler.101

	title = tbm_handler.101.name
	desc = tbm_handler.101.desc
	picture = GFX_evt_alien_planet
	show_sound = event_default

	is_triggered_only = yes
	location = this

	immediate = {
		add_modifier = {
			modifier = pioneering_colonists
			years = 20
		}
		
		if = {
			limit = {
				owner = {
					num_owned_planets < 3	# 3 because this is called after you own the colony. No luck for habitat starts, you already know a thing or two about colonization.
				}
			}
			add_modifier = {
				modifier = terra_incognita
				years = 20
			}
		}
		else = {
			add_modifier = {
				modifier = new_frontier
				years = 20
			}
		}

		# Currently, this does nothing. But one day.... (it will help with balance, too).
		set_timed_planet_flag = {
			flag = pioneer_planet
			years = 50
		}
	}

	option = {
		name = tbm_handler.101.option
	}
}

### 102. Wild World Generator (on_press_begin)
#	The long-term plan is that these shouldn't need to exist. Until then, they're added on_press_begin to provid flavor to and help ameliorate particularly nasty feature spawns, with the idea being to avoid planets with large numbers of negative district space because it confuses the UX.
event = {
	id = tbm_handler.102

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		log = "tbm_handler.102 - Wild World Generator"
		
		every_galaxy_planet = {
			limit = {
				NOT = { exists = owner }
				habitable_planet = yes
				num_free_districts = {
					type = district_city
					value < -3
				}
			}
			add_modifier = {
				modifier = wild_world
			}
		}
	}
}

### 103. Wild World Remover (on_yearly_pulse)
#	Removes the Wild World modifier when the planet has developed enough not to be one anymore.
planet_event = {
	id = tbm_handler.103

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		has_modifier = wild_world
		colony_age_years > 20
		num_free_districts = {
			type = district_city
			value >= 0
		}
	}

	immediate = {
		remove_modifier = wild_world
	}
}

### 104. Gestalt Wild World Adapter
#	on_colonizzation_started and on_planet_transfer
planet_event = {
	id = tbm_handler.104

	hide_window = yes
	is_triggered_only = yes

	trigger = {
		has_modifier = wild_world
		owner = { is_gestalt = yes }
	}

	immediate = {
		remove_modifier = wild_world
		add_modifier = {
			modifier = wild_world_gestalt
		}
	}
}

### 105. Pre-FTL Achieve FTL
#	Handles features for empires achieving FTL. Called from achieve_ftl_effect.
#	Vanilla Pre-FTLs clear their two Early Space Age blockers on reaching the Space Age via clear_blockers = yes, which wipes out all TBM features. This re-generates a standard starting spread.
planet_event = {
	id = tbm_handler.105

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		clear_deposits = yes
		if = {
			limit = {
				has_modifier = cleared_hot_springs
			}
			remove_modifier = cleared_hot_springs
		}
		add_planet_devastation = -100
		generate_start_deposits_and_blockers = yes
	}
}

### 106. Gestalt Colonial Buff
planet_event = {
	id = tbm_handler.106

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		if = {
			limit = {
				owner = { is_hive_empire = yes }
			}

			add_modifier = {
				modifier = new_branch
				years = 25
			}
		}
		else_if = {
			limit = {
				owner = { is_machine_empire = yes }
			}
			add_modifier = {
				modifier = new_function
				years = 25
			}
		}
		else = {
			add_modifier = {
				modifier = new_frontier
				years = 20
			}
		}
	}
}

### 110. Tomb World Features
#	Adds tomb world features when a planet is bombarded or otherwise converted into a Tomb World. Called from a couple of places, which is why it's separate.
planet_event = {
	id = tbm_handler.110

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		reroll_deposits = yes	#
	}
}


### 200. Terraforming Handler
#	Rerolls planet features on terraform. Does not clear anything other than what vanilla clears.
planet_event = {
	id = tbm_handler.200

	hide_window = yes
	is_triggered_only = yes

	trigger = {
		years_passed > 0	# Avoids this sometimes getting called at game start for reasons
	}

	immediate = {
		reroll_deposits = yes
		clean_up_clearance_modifiers = yes

		log = "Fired Handler 200 - rerolls deposits on terraform"

		if = {
			limit = {
				has_planet_flag = mitigated_urban_terraforming
			}
			planet_event = {
				id = tbm_handler.211
			}
		}
	}
}

### 210. Urban Terraforming
planet_event = {
	id = tbm_handler.210

	is_triggered_only = yes

	title = tbm_handler.210_name
	desc = {
		trigger = {
			owner = { is_gestalt = yes }
		}
		text = tbm_handler.210_desc_gestalt
	}
	desc = {
		trigger = {
			owner = { is_gestalt = no }
		}
		text = tbm_handler.210_desc
	}
	picture = GFX_evt_planet_beam
	show_sound = event_georadiation_terraforming

	trigger = {
		OR = {
			num_districts = {
				type = district_city
				value <= 3
			}

			pop_amount > 2000	# As you'll likely end up with a housing crunch and major problems.
		}
	}

	option = {
		name = tbm_handler.210_mitigate
		add_modifier = {
			modifier = mitigated_urban_terraforming
			years = 20
		}
		set_planet_flag = mitigated_urban_terraforming # In case you lose the modifier somehow
		custom_tooltip = tbm_handler.210_mitigation_tooltip
	}

	option = {
		name = tbm_handler.210_cancel
		cancel_terraformation = yes
	}

	option = {
		name = tbm_handler.210_ignore
		custom_tooltip = tbm_handler.210_ignore_tooltip
		ai_chance = {
			weight = 100	# Easier for compat and to avoid weird worlds. It can be beneficial for the AI to get district slots back this way. If it causes a rebellion, that tends to liven the game up for the player so not the worst thing, and it shouldn't hamper the AI economy too much.
		}
	}
}

### 211. Attempts to Fix Terraformed Urban Worlds
planet_event = {
	id = tbm_handler.211

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_planet_flag = mitigated_urban_terraforming
	}

	immediate = {
		while = {
			limit = {
				num_free_districts = {
					type = district_city
					value < 0
				}
			}

			random_deposit = {
				# Could set a limit here but we don't have a space list atm
				remove_deposit = yes
			}
		}

		log = "Finished Handler 211"

		clean_up_clearance_modifiers = yes
		remove_planet_flag = mitigated_urban_terraforming
	}
}

### 220. Ecumenopolis Handler
planet_event = {
	id = tbm_handler.220

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		
		# Store some values based on current planet for use later
		if = {
			limit = {
				OR = {
					has_any_factory_zone = yes
					has_any_foundry_zone = yes
					has_any_industrial_zone = yes
				}
			}
			set_planet_flag = ecu_zones_industrial
		}
		if = {
			limit = {
				has_any_foundry_zone = yes
			}
			set_planet_flag = ecu_zones_foundry
		}
		if = {
			limit = {
				has_any_factory_zone = yes
			}
			set_planet_flag = ecu_zones_factory
		}
		if = {
			limit = {
				OR = {
					has_any_unity_zone = yes
					has_any_research_zone = yes
				}
			}
			set_planet_flag = ecu_zones_technical
		}
		if = {
			limit = {
				planet_resource_compare = {
					resource = trade
					value > 800
				}
			}
			set_planet_flag = ecu_values_trade
		}
		if = {
			limit = {
				num_districts = {
					type = district_mining
					value > 6
				}
			}
			set_planet_flag = ecu_districts_mining
		}
		if = {
			limit = {
				num_districts = {
					type = district_generator
					value > 6
				}
			}
			set_planet_flag = ecu_districts_energy
		}
		if = {
			limit = {
				num_assigned_jobs = {
					job = bureaucrat
					value > 800
				}
			}
			set_planet_flag = ecu_jobs_bureaucracy
		}
		if = {
			limit = {
				free_amenities < 0
			}
			set_planet_flag = ecu_amenities_crunch
		}
		if = {
			limit = {
				free_housing < 100
			}
			set_planet_flag = ecu_housing_crunch
		}
		if = {
			limit = {
				planet_crime > 20
			}
			set_planet_flag = ecu_criminal_presence
		}
		
		
		# Wipe non-ecu features - necessary both as a fail-safe and also because a generator script might add something during the project construction
		every_deposit = {
			limit = {
				blocks_ecumenopolis = yes
			}
			remove_deposit = yes
		}

		# Feature Conversion
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_tbm_slums
						is_deposit_type = d_decrepit_dwellings
					}
				}
			}
			set_planet_flag = ecu_slum
			every_deposit = {
				limit = {
					OR = {
						is_deposit_type = d_tbm_slums
						is_deposit_type = d_decrepit_dwellings
					}
				}
				change_variable = {
					which = ecu_slums
					value = 1
				}
			}
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_black_market }
			}
			set_planet_flag = ecu_black_market
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_gas_vents }
			}
			set_planet_flag = ecu_gas
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_subterranean_gas_lake }
			}
			set_planet_flag = ecu_gas
		}
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_urban_aeroponics
						is_deposit_type = d_homeworld_urban_aeroponics
						is_deposit_type = d_homeworld_gestalt_urban_aeroponics
					}
				}
			}
			set_planet_flag = ecu_aeroponics
		}
		if = {
			limit = {
				any_deposit = { is_reactor_feature = yes }
			}
			set_planet_flag = ecu_reactor
			change_variable = {
				which = ecu_reactors
			}
		}
		if = {
			limit = {
				any_deposit = {	is_deposit_type = d_urban_power_plant }
			}
			set_planet_flag = ecu_reactor
			change_variable = {
				which = ecu_reactors
			}
		}
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_tbm_industrial_wasteland
						is_deposit_type = d_failing_infrastructure
						is_deposit_type = d_failing_infrastructure_earth
					}	
				}
			}
			set_planet_flag = ecu_industrial
			change_variable = {
				which = ecu_waste
			}
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_cesspool }
			}
			set_planet_flag = ecu_waste
			change_variable = {
				which = ecu_waste
			}
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_acidic_residue }
			}
			set_planet_flag = ecu_waste
			change_variable = {
				which = ecu_waste
			}
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_mesopotamian_urban_corridor }
			}
			set_planet_flag = ecu_mesopotamian
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_boswash_metropolitan_axis }
			}
			set_planet_flag = ecu_boswash
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_pearl_river_agglomerate }
			}
			set_planet_flag = ecu_pearl_river
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_mauritanian_security_zone }
			}
			set_planet_flag = ecu_mauritanian
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_scrap_bazaar }
			}
			set_planet_flag = ecu_scrap_bazaar
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_allocation_clinic }
			}
			set_planet_flag = ecu_allocation
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_memorial_apocalypse }
			}
			set_planet_flag = ecu_memorial
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_abyssal_manufactorium }
			}
			set_planet_flag = ecu_manufactorium
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_hegemons_of_industry }
			}
			set_planet_flag = ecu_hegemons
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_purgatory_apocalypse }
			}
			set_planet_flag = ecu_purgatory
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_last_bastion }
			}
			set_planet_flag = ecu_bastion
		}
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_tbm_crater
						is_deposit_type = d_crater
						is_deposit_type = d_rubble
						is_deposit_type = d_ruined_arcology
						is_deposit_type = d_ruined_district
						is_deposit_type = d_ruined_building_blocker
						is_deposit_type = d_tbm_impact_crater
					}
				}
			}
			set_planet_flag = ecu_debris			
			every_deposit = {
				limit = {
					OR = {
						is_deposit_type = d_tbm_crater
						is_deposit_type = d_crater
						is_deposit_type = d_rubble
						is_deposit_type = d_ruined_arcology
						is_deposit_type = d_ruined_district
						is_deposit_type = d_ruined_building_blocker
						is_deposit_type = d_tbm_impact_crater
					}
				}
				change_variable = {
					which = ecu_debris
					value = 1
				}
			}
		}
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_metal_boneyard
						is_deposit_type = d_city_ruins
						is_deposit_type = d_tomb_city
					}
				}
			}
			set_planet_flag = ecu_ruin
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_particle_storm }
			}
			set_planet_flag = ecu_particle
		}
		if = {
			limit = {
				any_deposit = { is_deposit_type = d_magma_lake }
			}
			set_planet_flag = ecu_magma
		}
		if = {
			limit = {
				any_deposit = {
					OR = {
						is_deposit_type = d_dustbowl
						is_deposit_type = d_homeworld_dustbowl
						is_deposit_type = d_dust_flats
					}
				}
			}
			set_planet_flag = ecu_dust
		}

		### Call repopulate and clean-up event with pulse delay
		planet_event = {
			id = tbm_handler.221
		}
	}
}

### 221. Ecu Repopulate and Cleanup
planet_event = {
	id = tbm_handler.221

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		# Repopulate Ecu Features and clear flags
		if = {
			limit = {
				has_planet_flag = ecu_slums	
			}
			while = {
				limit = {
					check_variable = {
						which = ecu_slums
						value > 0
					}
				}
				random_list = {
					75 = {
						add_deposit = d_tenement_arcologies
					}
					25 = {
						add_deposit = d_dusttown
						modifier = {
							factor = 0
							any_deposit = { is_deposit_type = d_dusttown }
						}
					}
				}
				subtract_variable = {
					which = ecu_slums
					value = 1
				}
			}
			remove_planet_flag = ecu_slums
			clear_variable = ecu_slums
		}
		if = {
			limit = {
				has_planet_flag = ecu_black_market
			}
			add_deposit = d_black_light_district
			remove_planet_flag = ecu_black_market
		}
		if = {
			limit = {
				has_planet_flag = ecu_gas
			}
			add_deposit = d_gas_wells
			remove_planet_flag = ecu_gas
		}
		if = {
			limit = {
				has_planet_flag = ecu_aeroponics
			}
			add_deposit = d_greenhouse_arcologies
			remove_planet_flag = ecu_aeroponics
		}
		if = {
			limit = { has_planet_flag = ecu_reactor }
			random_list = {
				75 = {
					add_deposit = d_reactor_chasm
				}
				25 = {
					add_deposit = d_reactor_chasm
				}
			}
			remove_planet_flag = ecu_reactor
		}
		if = {
			limit = {
				has_planet_flag = ecu_industrial
			}
			random_list = {
				75 = {
					add_deposit = d_industrial_expanse
				}
				5 = {
					add_deposit = d_mass_furnaces
				}
				15 = {
					add_deposit = d_suborbital_liftyard
				}
			}
			remove_planet_flag = ecu_industrial
		}
		if = {
			limit = {
				OR = {
					has_planet_flag = ecu_waste
					check_variable = {
						which = ecu_waste
						value > 0
					}
				}
			}
			while = {
				limit = {
					check_variable = {
						which = ecu_waste
						value > 0
					}
				}
				add_deposit = d_waste_lakes
			}
			remove_planet_flag = ecu_waste
			clear_variable = ecu_waste
		}
		if = {
			limit = {
				has_planet_flag = ecu_particle
			}
			add_deposit = d_particle_storm
			remove_planet_flag = ecu_particle
		}
		if = {
			limit = {
				has_planet_flag = ecu_debris
			}
			while = {
				limit = {
					check_variable = {
						which = ecu_debris
						value > 0
					}
				}
				random_list = {
					85 = {
						add_deposit = d_urban_debris
					}
					15 = {
						add_deposit = d_cracked_plate
						modifier = {
							factor = 0
							has_deposit = d_cracked_plate
						}
					}
				}
				subtract_variable = {
					which = ecu_debris
					value = 1
				}
			}
		}
		if = {
			limit = {
				has_planet_flag = ecu_ruins	
			}
			while = {
				limit = {
					check_variable = {
						which = ecu_debris
						value > 0
					}
				}
				random_list = {
					85 = {
						add_deposit = d_urban_debris
					}
					15 = {
						add_deposit = d_cracked_plate
						modifier = {
							factor = 0
							has_deposit = d_cracked_plate
						}
					}
				}
				subtract_variable = {
					which = ecu_debris
					value = 1
				}
			}
		}

		# Earth Swaps
		if = {
			limit = {
				has_planet_flag = ecu_mesopotamian
			}
			add_deposit = d_mesopotamian_urban_archway
			remove_planet_flag = ecu_mesopotamian
		}
		if = {
			limit = {
				has_planet_flag = ecu_boswash
			}
			add_deposit = d_boswash_trade_zone
			remove_planet_flag = ecu_boswash
		}
		if = {
			limit = {
				has_planet_flag = ecu_pearl_river
			}
			add_deposit = d_pearl_river_lattice
			remove_planet_flag = ecu_pearl_river
		}
		if = {
			limit = {
				has_planet_flag = ecu_mauritanian
			}
			add_deposit = d_mauritanian_memorial
			remove_planet_flag = ecu_mauritanian
		}

		# Post-Apoc Features - these are restored if present so you don't lose them
		if = {
			limit = {
				has_planet_flag = ecu_scrap_bazaar
			}
			add_deposit = d_fusion_bazaar
			remove_planet_flag = ecu_scrap_bazaar
		}
		if = {
			limit = {
				has_planet_flag = ecu_allocation
			}
			add_deposit = d_allocation_clinic
			remove_planet_flag = ecu_allocation
		}
		if = {
			limit = {
				has_planet_flag = ecu_memorial
			}
			add_deposit = d_memorial_apocalypse
			remove_planet_flag = ecu_memorial
		}
		if = {
			limit = {
				has_planet_flag = ecu_manufactorium
			}
			add_deposit = d_abyssal_manufactorium
			remove_planet_flag = ecu_mesopotamian
		}
		if = {
			limit = {
				has_planet_flag = ecu_hegemons
			}
			add_deposit = d_hegemons_of_industry
			remove_planet_flag = ecu_hegemons
		}
		if = {
			limit = {
				has_planet_flag = ecu_purgatory
			}
			add_deposit = d_purgatory_apocalypse
			remove_planet_flag = ecu_hegemons
		}
		if = {
			limit = {
				has_planet_flag = ecu_bastion
			}
			add_deposit = d_last_bastion
			remove_planet_flag = ecu_bastion
		}


		### Randomize Ecu Features
		tbm_ecumenopolis_feature_generation = yes

		remove_planet_flag = ecu_zones_industrial
		remove_planet_flag = ecu_zones_factory
		remove_planet_flag = ecu_zones_foundry
		remove_planet_flag = ecu_zones_technical
		remove_planet_flag = ecu_values_trade
		remove_planet_flag = ecu_jobs_bureaucracy
		remove_planet_flag = ecu_districts_energy
		remove_planet_flag = ecu_districts_mining
		remove_planet_flag = ecu_amenities_crunch
		remove_planet_flag = ecu_housing_crunch
		remove_planet_flag = ecu_criminal_presence

		# Clean-up
		clean_up_clearance_modifiers = yes
	}
}

### 230. Habitat Feature Generation
#	Player-facing to allow some control of specialization
country_event = {
	id = tbm_handler.230

	is_triggered_only = yes
	location = fromfrom
	
	title = tbm_handler.230_name
	desc = tbm_handler.230_desc
	picture = GFX_evt_habitat
	show_sound = pc_habitat

	immediate = {
		
		fromfrom = {
			random_system_planet = {
				limit = { is_planet_class = pc_habitat  NOT = { has_planet_flag = has_habitat_features } }
				hidden_effect = {
					generate_general_habitat = yes
					set_planet_flag = has_habitat_features		# This prevents the above check from firing on an already-constructed hab if the user uses a mod that allows for multiple in the system.

					save_event_target_as = constructed_habitat
				}
			}
			
		}
		
		log = "Called tbm_handler.230 to generate hab features."
	}

	# Emphasize Military Features
	option = {
		name = tbm_habitat_military_purposes
		custom_tooltip = generate_military_hab_tooltip

		event_target:constructed_habitat = {
			set_planet_flag = habitat_military
			hidden_effect = {
				generate_military_habitat = yes
			}
		}
	}

	# Emphasize Trade and Industry
	option = {
		name = tbm_habitat_civilian_outpost
		event_target:constructed_habitat = {
			set_planet_flag = habitat_civilian
			hidden_effect = {
				generate_civilian_habitat = yes
			}
		}
		custom_tooltip = generate_civilian_hab_tooltip
	}

	# Crates Agricultural Features
	option = {
		name = tbm_habitat_farm
		custom_tooltip = geneerate_farming_hab_toioltip

		event_target:constructed_habitat = {
			set_planet_flag = habitat_farm
			hidden_effect = {
				generate_farming_habitat = yes
			}
		}
	}

	# Lease Station to Corporation
	option = {
		name = tbm_habitat_lease
		custom_tooltip = generate_corporate_hab_tooltip

		event_target:constructed_habitat = {
			set_planet_flag = habitat_corporate
			hidden_effect = {
				generate_leased_habitat = yes
			}
		}
	}
}

### 240. Habitat Administrative Secion added on_colonization_completr - this avoids having a lame duck feature in the colonization preview
planet_event = {
	id = tbm_handler.240

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		add_deposit = d_administrative_section
	}
}


### 301. Wenkwort Handler
#	on_colonization_started and on_blocker_cleared (which is in planet scope)
planet_event = {
	id = tbm_handler.301

	is_triggered_only = yes
	hide_window = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		exists = owner
		owner = { is_ai = no } # For now, anyway. Suspect AI probably won't do that well with the shifting nature of this event, plus they usually pave the place in vanilla anyway.
		this = {
			is_same_value = event_target:wenkwort_artem 	# global_event_target
		}
	}

	immediate = {
		every_deposit = {
			limit = {
				NOT = { has_deposit_flag = wenkwort_deposit }
			}
			set_deposit_flag = wenkwort_deposit
		}

		# on_blocker_cleared handling (from == deposit )
		if = {
			limit = {
				# Confirmed, I think, that from == deposit in synth.311, under machine_age_events, where any_deposit checks is_same_value = from
				AND = {
					from = { is_scope_type = deposit }
					from = { has_deposit_flag = wenkwort_deposit }
				}
				
				# Don't fire negative events until colony is up and running
				pop_amount > 500
			}

			change_variable = {
				which = wenkwort_custodianship
				value = -1
			}

			random_list = {
				33 = {
					modifier = {
						factor = 0
						has_modifier = pm_wenkwort_disease_outbreak
					}
					
					planet_event = {
						id = tbm.505
						days = 60
						random = 30
					}
				}
				
				33 = {
					modifier = {
						factor = 0
						has_modifier = pm_wenkwort_invasive_species
					}
					
					planet_event = {
						id = tbm.506
						days = 60
						random = 30
					}
				}

				33 = {
					modifier = {
						factor = 0
						has_modifier = pm_wenkwort_predatory_biology
					}
					
					planet_event = {
						id = tbm.507
						days = 60
						random = 30
					}
				}
			}
		}

		# Replaces vanilla Wenkwort Custodian once it's colonized
		if = {
			limit = {
				exists = owner
				NOT = { has_planet_flag = fixed_wenkwort_modifiers }
			}
			
			this = {
				planet_event = {
					id = tbm.509
					days = 20 # Need to get it out of the way of the other Wenkwort events, which are called eight days after on_colonization_started, so that tbm.509 can overwrite the vanilla modifier.
				}
			}
		}
	}
}


### Urban Feature Generators

### 401. Black Market Handler
planet_event = {
	id = tbm_handler.401

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		OR = {
			has_modifier = criminal_underworld
			AND = {
				planet_crime > 10
				planet_resource_compare = {
					resource = trade
					value > 100
				}
			}
		}
	}

	immediate = {
		random_list = {
			10 = {
				
				modifier = {
					factor = 0
					owner = {
						NOT = { is_regular_empire = yes }
					}
				}
				
				planet_event = {
					id = tbm_handler.402
					days = 180
					random = 180
				}
			}
			90 = {}
		}
	}
}

### 402. Black Market Generator
planet_event = {
	id = tbm_handler.402

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = black_market_generator_message
			days = 30
			target = root
		}

		add_deposit = d_black_market
	}
}

### 403. Spire District Handler
planet_event = {
	id = tbm_handler.403

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_major_upgraded_capital = yes
		num_cities = { $COUNT$ = 4 }
		NOT = { has_deposit = d_spire_district }
	}

	immediate = {
		random_list = {
			1 = {
				planet_event = {
					id = tbm_handler.404
					days = 180
					random = 180
				}
			}
			99 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.404

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = spire_district_generator_message
			days = 30
			target = root
		}

		add_deposit = d_spire_district
	}
}

### 405. Slime Ocean handler
planet_event = {
	id = tbm_handler.405

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_industrial_zoning = yes
		num_cities = { $COUNT$ = 8 }
		NOT = { has_deposit = d_slime_ocean } # You'd presumably just keep on dumpin'....
	}

	immediate = {
		random_list = {
			4 = {
				planet_event = {
					id = tbm_handler.406
					days = 180
					random = 180
				}
			}
			96 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.406

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = slime_ocean_generator_message
			days = 30
			target = root
		}

		add_deposit = d_slime_ocean
	}
}

### 407. Terrace Handler
planet_event = {
	id = tbm_handler.407

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					factor = 2
					owner = { has_technology = tech_housing_1 }
				}
				modifier = {
					factor = 2
					owner = { has_technology = tech_housing_2 }
				}
				modifier = {
					factor = 0.01
					count_deposits = {
						type = d_terraces
						count >= 4
					}
				}

				planet_event = {
					id = tbm_handler.408
				}
			}
			95 = {}
		}
	}
}

### 408. Terrace Generator
planet_event = {
	id = tbm_handler.408

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = terrace_generator_message
			days = 30
			target = root
		}

		add_deposit = d_terraces
	}
}

### 409. Urban Wilderness Handler
planet_event = {
	id = tbm_handler.409

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 8 }
		has_industrial_zoning = no
		NOT = {
			count_deposits = {
				count <=1
				type = d_urban_wilderness
			}
		}
		num_free_districts = {
			type = district_city
			value >= 1
		}
	}

	immediate = {
		random_list = {
			2 = {
				planet_event = {
					id = tbm_handler.410
					days = 180
					random = 180
				}
			}
			98 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.410

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = urban_wilderness_generator_message
			days = 30
			target = root
		}

		remove_district = district_city
		add_deposit = d_urban_wilderness
	}
}

### 411. Mag-Lev Network Generator
planet_event = {
	id = tbm_handler.411

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_major_upgraded_capital = yes
		num_cities = { $COUNT$ = 6 }
		NOT = { has_deposit = d_mag_lev_network }
	}

	immediate = {
		random_list = {
			20 = {
				planet_event = {
					id = tbm_handler.412
					days = 180
					random = 180
				}
			}
			80 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.412

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = mag_lev_generator_message
			days = 30
			target = root
		}

		add_deposit = d_mag_lev_network
	}
}

### 413. Alley Generator
planet_event = {
	id = tbm_handler.413

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
	}

	immediate = {
		random_list = {
			10 = {
				modifier = {
					factor = 0.1
					count_deposits = {
						count >= 4
						type = d_alleys
					}
				}

				planet_event = {
					id = tbm_handler.414
					days = 180
					random = 180
				}
			}
		}
	}
}

planet_event = {
	id = tbm_handler.414

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = alley_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_alleys
	}
}

### 415. Bazaar Generator
planet_event = {
	id = tbm_handler.415

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 5 }
		NOT = {
			count_deposits = {
				count >= 2
				type = d_bazaar
			}
		}
	}

	immediate = {
		random_list = {
			20 = {
				modifier = {
					factor = 0.2
					owner = {
						has_authority = auth_corporate
					}
				}

				modifier = {
					factor = 0
					owner = {
						NOT = { is_regular_empire = yes }
					}
				}
				
				planet_event = {
					id = tbm_handler.416
					days = 180
					random = 180
				}
			}
		}
	}
}

planet_event = {
	id = tbm_handler.416

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = bazaar_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_bazaar
	}
}

### 417. Warehouse Generator
planet_event = {
	id = tbm_handler.417

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
		NOT = {
			count_deposits = {
				count >= 6
				type = d_warehouses
			}
		}
	}

	immediate = {
		random_list = {
			4 = {
				modifier = {
					factor = 2
					has_industrial_zoning = yes
				}

				planet_event = {
					id = tbm_handler.418
					days = 180
					random = 180
				}
			}
			96 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.418

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = warehouse_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_warehouses
	}
}

### 419. Port Complex Generator
planet_event = {
	id = tbm_handler.419

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		OR = {
			is_planet_class = pc_ocean
			is_planet_class = pc_continental
			is_planet_class = pc_tropical
			is_planet_class = pc_gaia
			is_planet_class = pc_arctic
		}
		num_cities = { $COUNT$ = 3 }
		NOT = { has_deposit = d_port_complex }
	}

	immediate = {
		random_list = {
			5 = {
				planet_event = {
					id = tbm_handler.420
					days = 180
					random = 180
				}
			}
			95 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.420

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = port_complex_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_port_complex
	}
}

### 421. Reservoir Generator
planet_event = {
	id = tbm_handler.421

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_industrial_zoning = yes
		num_cities = { $COUNT$ = 4 }
		
		# Dry worlds will get these more often, and can potentially run into space problems with them.
		OR = {
			num_free_districts = {
				type = district_city
				value >= 3
			}
			is_dry = yes
		}
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					factor = 2
					is_dry = yes
				}
				modifier = {
					factor = 0.5
					num_free_districts = {
						type = district_city
						value < 2
					}
				}

				planet_event = {
					id = tbm_handler.422
					days = 180
					random = 180
				}
			}
			95 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.422

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = reservoir_generator_message
			days	 = 30
			target = root
		}	

		if = {
			limit = {
				num_free_districts = {
					type = district_city
					value < 1
				}
			}
			remove_district = district_city
		}
		add_deposit = d_reservoirs
	}
}

### 423. Parlor Generator
planet_event = {
	id = tbm_handler.423

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 4 }
		NOT = {
			count_deposits = {
				count >= 2
				type = d_parlors
			}
		}
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					factor = 2
					OR = {
						has_any_unity_zone = yes
						has_any_trade_zone = yes
					}
				}

				modifier = {
					factor = 2
					has_major_upgraded_capital = yes
				}

				planet_event = {
					id = tbm_handler.424
					days = 180
					random = 180
				}
			}
			95 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.424

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = parlor_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_parlors
	}
}

### 425. Xeno Quarter Generator
planet_event = {
	id = tbm_handler.425

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		count_owned_species = {
			count >= 2
			limit = {
				NOT = { is_subspecies = owner_species }
				NAND = {
					is_robotic_species = yes
					owner = { has_tech_synthetic_workers_prerequisites = no }
				}
			}
		}
		num_cities = { $COUNT$ = 3 }
		NOT = {
			has_deposit = d_xeno_quarter
		}
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					factor = 2
					owner = {
						OR = {
							is_xenophile = yes
							is_xenophobe = yes
						}
					}
				}

				planet_event = {
					id = tbm_handler.426
					days = 180
					random = 180
				}
			}
			95 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.426

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = xeno_quarter_message
			days	 = 30
			target = root
		}	

		add_deposit = d_xeno_quarter
	}
}

### 427. Casino Generator
planet_event = {
	id = tbm_handler.427

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
		has_major_upgraded_capital = yes
		NOT = {
			count_deposits = {
				count >= 3
				type = d_casinos
			}
		}
	}

	immediate = {
		random_list = {
			4 = {
				planet_event = {
					id = tbm_handler.428
					days = 180
					random = 180
				}
			}
			96 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.428

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = casino_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_casinos
	}
}

### 429. Theater Generator
planet_event = {
	id = tbm_handler.429

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
		has_major_upgraded_capital = yes
		NOT = { has_deposit = d_theater_district }
	}

	immediate = {
		random_list = {
			3 = {
				planet_event = {
					id = tbm_handler.430
					days = 180
					random = 180
				}
			}
			97 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.430

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = theater_message_generator
			days	 = 30
			target = root
		}	

		add_deposit = d_theater_district
	}
}

### 431. Residential Tower Generator
planet_event = {
	id = tbm_handler.431

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 6 }
		free_housing < 1000
		has_upgraded_capital = yes
		NOT = {
			count_deposits = {
				count >= 8
				type = d_residential_towers
			}
		}
	}

	immediate = {
		random_list = {
			5 = {
				modifier = {
					factor = 2
					free_housing < 0
				}

				planet_event = {
					id = tbm_handler.432
					days = 180
					random = 180
				}
			}
		}
	}
}

planet_event = {
	id = tbm_handler.432

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = residential_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_residential_towers
	}
}

### Mega-City Generator
planet_event = {
	id = tbm_handler.433

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 10 }
		has_any_trade_zone = yes
		OR = {
			has_major_upgraded_capital = yes
			is_homeworld_or_lost_colony = yes
		}
		NOT = {
			count_deposits = {
				count >= 4
				type = d_commercial_megacity
			}
		}
	}

	immediate = {
		random_list = {
			2 = {
				modifier = {
					factor = 2
					planet_crime < 15
				}
				
				planet_event = {
					id = tbm_handler.434
					days = 180
					random = 180
				}
			}
			98 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.434

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = mega_city_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_commercial_megacity
	}
}

### Financial Sector Generator
planet_event = {
	id = tbm_handler.435

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 10 }
		has_any_trade_zone = yes
		OR = {
			has_fully_upgraded_capital = yes
			is_homeworld_or_lost_colony = yes
			is_planet_class = pc_city
		}
		NOT = { has_deposit = d_financial_sector }
	}

	immediate = {
		random_list = {
			2 = {
				modifier = {
					factor = 2
					planet_crime < 20
				}

				planet_event = {
					id = tbm_handler.436
					days = 180
					random = 180
				}
			}
			98 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.436

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = financial_sector_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_financial_sector
	}
}

### Substation Generator
planet_event = {
	id = tbm_handler.437

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 3 }
		has_upgraded_capital = yes
		NOT = {
			count_deposits = {
				count >= 3
				type = d_substation
			}
		}
	}

	immediate = {
		random_list = {
			10 = {
				planet_event = {
					id = tbm_handler.438
					days = 180
					random = 180
				}
			}
			90 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.438

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = substation_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_substation
	}
}

### 449. Boulevard Generator
planet_event = {
	id = tbm_handler.439

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		num_cities = { $COUNT$ = 3 }
		has_upgraded_capital = yes
		NOT = {
			count_deposits = {
				count >= 2
				type = d_boulevard
			}
		}
	}

	immediate = {
		random_list = {
			5 = {
				planet_event = {
					id = tbm_handler.440
					days = 180
					random = 180
				}
			}
			95 = {}
		}
	}
}

planet_event = {
	id = tbm_handler.440

	is_triggered_only = yes
	hide_window = yes

	immediate = {
		create_message = {
			type = URBAN_DEVELOPMENT
			localization = boulevard_generator_message
			days	 = 30
			target = root
		}	

		add_deposit = d_boulevard
	}
}


### 500 - HABITAT FEATURE EVENTS

### 501. Change Habitat Docks
planet_event = {
	id = tbm_handler.501

	is_triggered_only = yes

	title = tbm_handler.501_name
	desc = tbm_Handler.501_desc
	picture = GFX_evt_habitat
	show_sound = event_construction

	immediate = {
		set_timed_planet_flag = {
			flag = dock_expansion_cooldown
			years = 5
		}
	}

	option = {
		name = add_military_docks
		allow = {
			NOT = { has_deposit = d_military_docks }
		}

		owner = {
			add_resource = {
				alloys = -2500
				energy = -2000
				minerals = -800
			}
		}

		add_deposit = d_military_docks
	}

	option = {
		name = add_tourist_docks
		allow = {
			NOT = { has_deposit = d_tourist_promenade }
		}

		owner = {
			add_resource = {
				alloys = -1500
				energy = -2000
				minerals = -800
				trade = -500
			}
		}

		add_deposit = d_tourist_promenade
	}

	option = {
		name = add_capital_docks
		allow = {
			NOT = { has_deposit = d_capital_ship_pier }
		}

		owner = {
			add_resource = {
				alloys = -1500
				energy = -2000
				minerals = -1200
			}
		}

		add_deposit = d_capital_ship_pier
	}
}